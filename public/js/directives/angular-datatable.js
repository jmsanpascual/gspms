'use strict'

var dtTable = angular.module('dtTable.directives', []);


dtTable.directives('dataTable', function(){
	return {
		restrict : 'A',
		// template:
		link: function(scope, element, attr)
        {
          	
        } 
	}
});

dtTable.directives('generateRows', function(){
	return {
		
	}
});

dtTable.directives('pagination', function(){

});